{{! views/produtos.handlebars }}

<h1>Lista de Produtos</h1>

{{#if produtos}}
    <table>
        <thead>
            <tr>
                <th>Nome</th>
                <th>Preço</th>
            </tr>
        </thead>
        <tbody>
            {{#each produtos}}
            <tr>
                <td>{{nome}}</td>
                <td>R$ {{preco}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
    <p>Nenhum produto cadastrado.</p>
{{/if}}

<p><a href="/produtos/novo">Adicionar Novo Produto</a></p>